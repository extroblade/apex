server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    # Static assets (default avatars, etc.) — cache aggressively.
    location / {
        add_header Cache-Control "public, max-age=86400";
        try_files $uri =404;
    }
}
